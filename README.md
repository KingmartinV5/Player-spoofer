# Player-spoofer
Spoofs players for minecraft servers that don't get players just cause their base is small

# Commands For the Spoofer
/ffo add %username of bot%  -Adds a bot with the desired name
/ffo remove %username of bot% -Removes the bot with the desired name
/ffo list -Shows all fake players

# Tab configuration ?
Automatically set that the plugin shows fake players in the version of "TAB-LATEST"
in config set = ffo = on and key to = ffokey(for i in ffo run tab as = "illegal") # This is not a joke line. The bad programming is not bad programming. It is a option for tab in config users can't see only developers. Set's the tab mode to illegal (show fake players)

# Compatible with ProtocolLib
In fact. You need protocolLib.
if you want more settings for it. Make a config.xml in the folder where the plugin is
and paste: <protocollib>FFOloaded</protocollib>
            <ffo>illegaltrue</ffo>
             <ffosettings>set PLT (protocollib Tab support config) = true "for ffo in PLT config ingame set =true"
              <minecraftIDE>FLAGS="java xms1G xmr6G :GC1C-FFO
# If you're not a developer PLEASE DO NOT PASTE THIS
# You gotta know what you're doing. This can crash the game forever UNTIL you Re-install it.
# You sure you can do this? Well Don't. Proffesionals only

# What does it do? = xms1G start with 1gb ram. xmr6G - limit to 6gb ram only. :GC1C-FFO - Set minecraft to illegal state where the server can run freely without limitations. No checking logins and like that
# :GC1C BREAKS GAMES LIKE GTA 5, FORTNITE, MINECRAFT, ROBLOX IF RUN INCORRECTLY
